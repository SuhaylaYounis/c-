#include <iostream>
using namespace std; //program to get the max number in array
int maxint() //making a new function called maxint
{
    int i, n; //declaring datatypes of count and the total number of elements in the array
    int arr[100];
    cout << "Enter the number of elements in array: "; //prompt user to enter the total number of elements in the array
    cin >> n;
    cout << endl;
    for (i = 0; i < n; i++)  //user enters the values of the numbers of each element
    {
        cout << "number" << i + 1 << "is: ";
        cin >> arr[i];
    }
    for (i = 1; i < n; i++) //checking which number is the maxium in th array
    {
        if (arr[0] < arr[i])
            arr[0] = arr[i];
    }
    return arr[0]; //returning the value of the max element
}   
int main()
{
    cout << "The maxiumum number is : " << maxint(); //printing the maximum value to console screen by calling the function maxint()

}